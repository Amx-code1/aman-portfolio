
import { animate, timeline, stagger, spring, inView, scroll } from 'motion';

/**
 * Curated Motion One toolkit. Lightweight, performant primitives used for
 * energy pulses, cursor effects, and background movement across the engine.
 */
export const mo = {
  animate,
  timeline,
  stagger,
  spring,
  inView,
  scroll,
};
